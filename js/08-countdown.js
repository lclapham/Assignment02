// Count backwards from whatever number the user wants to count down from.
let userNum = window.prompt("Enter any number to count down from")
for (i = userNum; i >= 0; i--) {
    window.console.log(i);
}